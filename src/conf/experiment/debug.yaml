# @package _global_

# Debug configuration
# Use base model


defaults:
  - override /datasets: litbank
  - override /trainer: train.yaml
  - override /model/doc_encoder/transformer: longformer_base

trainer:
  log_frequency: 10
  max_evals: 5
  patience: 2
  eval_per_k_steps: 20

#model:
#  doc_encoder:
#    transformer: lonformer_base

